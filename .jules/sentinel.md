Learned that DOMParser is not sufficient to prevent XSS from inline event handlers when appending nodes; we must rewrite HTML generation to use document.createElement, classList, and textContent exclusively to ensure true security.
